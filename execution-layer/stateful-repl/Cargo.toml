[package]
name = "stateful-repl"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.34", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
anyhow = "1.0"
# Represents network namespace sharing and socket tunneling for REPL
